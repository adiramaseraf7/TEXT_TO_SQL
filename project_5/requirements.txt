# Local (no GPU needed): data prep, execution evaluator, demo app
datasets>=2.19.0
pandas>=2.0.0
gradio>=4.30.0
requests>=2.31.0
pytest>=8.0.0

# Colab / GPU fine-tuning (install separately in the fine-tuning notebook)
# torch
# transformers>=4.41.0
# peft>=0.11.0
# trl>=0.9.4
# bitsandbytes>=0.43.0
# accelerate>=0.30.0
